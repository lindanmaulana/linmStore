const IconsSosmed = (props) => {
    const {children} = props
  return (
    <a href="https://www.facebook.com/qqqqmln" className="scale-150">
      {children}
    </a>
  );
};

export default IconsSosmed;
